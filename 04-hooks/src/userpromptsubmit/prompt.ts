/**
 * UserPromptSubmit hook test prompt
 * UserPromptSubmit fires for every user prompt before Claude processes it
 */
export const prompt = "What is the capital of France?";
export const description = "UserPromptSubmit fires for every user prompt submission";
export const hookName = "UserPromptSubmit";
