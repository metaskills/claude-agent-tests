/**
 * Notification hook test prompt
 * Notification fires when Claude Code sends notifications (may not fire in all scenarios)
 * Note: This hook may be difficult to trigger reliably in automated tests
 */
export const prompt = "What is 2 + 2?";
export const description = "Notification fires when SDK sends notifications (may not trigger in all scenarios)";
export const hookName = "Notification";
